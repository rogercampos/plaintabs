%h1 Listing tabs
%table
  %tr
    %th Title
    %th Band
    %th
    %th
    -#%th
  - @tabs.each do |tab|
    %tr
      %td= tab.title
      %td= tab.band
      %td= link_to 'Show', tab
      %td= link_to 'Edit', edit_tab_path(tab)
      -#%td= link_to 'Destroy', tab, :confirm => 'Are you sure?', :method => :delete
%br/
= link_to 'New Tab', new_tab_path
